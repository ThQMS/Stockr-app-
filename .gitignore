.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
coverage/
# pubspec.lock IS committed for reproducible builds (this is an app, not a package).

android/.gradle/
android/local.properties
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
ios/Pods/
macos/Pods/

*.iml
.idea/
.vscode/

# Android signing / secrets — must never be committed
*.jks
*.keystore
key.properties
android/key.properties
**/*.env
.env
.env.*
secrets.dart
lib/**/secrets.dart

# Generated code (produced by build_runner; regenerated in CI)
*.g.dart
*.freezed.dart
*.mocks.dart
# NOTE: lib/core/di/injection.config.dart is hand-written DI wiring (no
# @InjectableInit annotations exist), so it IS committed — not generated.

# OS noise
.DS_Store
Thumbs.db
